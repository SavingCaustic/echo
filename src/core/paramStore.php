<?php
//reading json files, and store them in ram for fast retrivial.
//hang on, maybe we could have these as class variables and not object variables?

class ParamStore {
    var $numParams;

    function __construct() {

    }

    function str2int($param) {
        //maybe use this for c++ switch support in code.
    }


}